name: 'my_new_project'
version: '1.0.0'
config-version: 2        

profile: 'my_new_project'    

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  build_mau:
    input:
      +materialized: ephemeral
    output:
      +materialized: table