/**
 * ============================================
 * MODELS INDEX
 * ============================================
 * 
 * Central export point for all models
 * Makes importing models easier
 */

const Book = require('./Book');

module.exports = {
  Book
};
