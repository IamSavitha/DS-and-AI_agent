<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard - Session Authentication</title>
    <!-- Bootstrap CSS for styling -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">
    <!-- Navigation Bar -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container">
            <a class="navbar-brand" href="/">Session Auth Demo</a>
            <div class="navbar-nav ms-auto">
                <!-- Display username in navbar -->
                <span class="navbar-text me-3">Welcome, <strong><%= user.username %></strong></span>
                <!-- Logout link -->
                <a class="nav-link" href="/auth/logout">Logout</a>
            </div>
        </div>
    </nav>

    <div class="container mt-5">
        <div class="row justify-content-center">
            <div class="col-md-8">
                <div class="card shadow">
                    <div class="card-body">
                        <h1 class="card-title text-center mb-4">Dashboard</h1>
                        
                        <!-- Welcome Message -->
                        <!-- EJS template syntax: <%= %> outputs the value -->
                        <p class="text-center">Welcome, <strong><%= user.username %></strong>!</p>
                        
                        <!-- Session Information Card -->
                        <div class="card mt-4">
                            <div class="card-header">
                                <h5>Session Information</h5>
                            </div>
                            <div class="card-body">
                                <p><strong>User ID:</strong> <%= user.id %></p>
                                <p><strong>Username:</strong> <%= user.username %></p>
                                <p class="text-muted">
                                    <small>
                                        Your session is stored server-side and identified by a cookie.
                                        This session will expire after 24 hours of inactivity.
                                    </small>
                                </p>
                            </div>
                        </div>
                        
                        <!-- Action Buttons -->
                        <div class="d-grid gap-2 mt-4">
                            <a href="/auth/logout" class="btn btn-danger">Logout</a>
                            <a href="/" class="btn btn-secondary">Home</a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Bootstrap JS for interactive components -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
