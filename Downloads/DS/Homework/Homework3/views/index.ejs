<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home - Session Authentication</title>
    <!-- Bootstrap CSS for styling -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">
    <!-- Navigation Bar -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container">
            <a class="navbar-brand" href="/">Session Auth Demo</a>
            <div class="navbar-nav ms-auto">
                <!-- Conditional rendering based on authentication status -->
                <% if (user) { %>
                    <!-- User is logged in -->
                    <span class="navbar-text me-3">Welcome, <strong><%= user.username %></strong></span>
                    <a class="nav-link" href="/dashboard">Dashboard</a>
                    <a class="nav-link" href="/auth/logout">Logout</a>
                <% } else { %>
                    <!-- User is not logged in -->
                    <a class="nav-link" href="/auth/login">Login</a>
                <% } %>
            </div>
        </div>
    </nav>

    <div class="container mt-5">
        <div class="row justify-content-center">
            <div class="col-md-10">
                <div class="card shadow">
                    <div class="card-body">
                        <h1 class="card-title text-center mb-4">Session-Based Authentication Demo</h1>
                        
                        <!-- Conditional Content Based on Authentication -->
                        <% if (user) { %>
                            <!-- Authenticated User View -->
                            <div class="alert alert-success" role="alert">
                                <h4 class="alert-heading">You are logged in!</h4>
                                <p>Welcome back, <strong><%= user.username %></strong>!</p>
                                <hr>
                                <p class="mb-0">
                                    <a href="/dashboard" class="btn btn-primary">Go to Dashboard</a>
                                </p>
                            </div>
                        <% } else { %>
                            <!-- Guest User View -->
                            <div class="alert alert-info" role="alert">
                                <h4 class="alert-heading">Welcome!</h4>
                                <p>Please log in to access the dashboard.</p>
                                <hr>
                                <p class="mb-0">
                                    <a href="/auth/login" class="btn btn-primary">Login</a>
                                </p>
                            </div>
                        <% } %>
                        
                        <!-- Information Section -->
                        <div class="mt-4">
                            <h3>About This Application</h3>
                            <p>This is a demonstration of <strong>session-based authentication</strong> using:</p>
                            <ul>
                                <li><strong>Express.js</strong> - Web application framework</li>
                                <li><strong>express-session</strong> - Session management middleware</li>
                                <li><strong>bcryptjs</strong> - Password hashing library</li>
                                <li><strong>EJS</strong> - Embedded JavaScript templating</li>
                            </ul>
                            
                            <h4 class="mt-4">Key Concepts:</h4>
                            <ul>
                                <li><strong>Server-side Sessions:</strong> Session data stored on the server</li>
                                <li><strong>Session Cookies:</strong> Client receives a session ID cookie</li>
                                <li><strong>Password Hashing:</strong> Passwords are hashed using bcrypt</li>
                                <li><strong>Protected Routes:</strong> Routes that require authentication</li>
                            </ul>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Bootstrap JS for interactive components -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
